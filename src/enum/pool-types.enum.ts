export enum PoolTypesEnum {
  HomePool = 'Home Pool',
  HousingCommunityPool = 'Housing Community Pool',
  HotelPool = 'Hotel Pool',
  PublicPool = 'Public Pool',
  MembersOnlyPool = 'Members Only Pool',
  OtherPool = 'Other Pool',
}
